# Core LLM + Agent framework
langchain-openai
langchain-core
langgraph

# Slack
slack-sdk

# Jira
requests

# Google (Gmail + Calendar + Drive)
google-auth
google-auth-httplib2
google-auth-oauthlib
google-api-python-client
python-dateutil

# Data validation
pydantic
email-validator

# ── Meeting Summarizer additions ──────────────────────────────
# AWS SDK (S3 lock, SES email, artifact storage)
boto3

# ICS calendar invite generation
icalendar

# Timezone conversion for UTC fix (FIX-3)
pytz
# ─────────────────────────────────────────────────────────────

# Environment (local only — not needed in Lambda but harmless)
python-dotenv
